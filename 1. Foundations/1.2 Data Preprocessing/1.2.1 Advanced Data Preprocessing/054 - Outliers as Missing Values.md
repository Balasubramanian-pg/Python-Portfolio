# Outliers as Missing Values

A hybrid strategy where identified extreme outliers are converted to NaNs and then handled using advanced imputation techniques (like MICE) to estimate a reasonable value.
