# scikit-learn Pipeline (Pipeline)

A sequential object that chains together multiple preprocessing steps and a final estimator, guaranteeing that preprocessing logic is sequentially executed without leakage during cross-validation.
