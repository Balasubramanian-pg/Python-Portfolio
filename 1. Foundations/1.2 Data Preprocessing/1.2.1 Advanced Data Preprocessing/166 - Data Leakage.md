# Data Leakage

The catastrophic error of allowing information from outside the training dataset (specifically the validation/test set or future temporal data) to influence the preprocessing parameters.
