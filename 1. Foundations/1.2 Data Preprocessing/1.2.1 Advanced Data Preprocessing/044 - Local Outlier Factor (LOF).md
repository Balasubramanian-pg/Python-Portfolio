# Local Outlier Factor (LOF)

A density-based algorithm that compares the local density of a point to the local densities of its neighbors, identifying points that are substantially less dense (anomalies).
