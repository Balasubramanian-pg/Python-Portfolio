# Hash Collisions

The main drawback of feature hashing where two distinct categories are mapped to the same column; usually tolerated because machine learning models can often unentangle the signal.
