# Contextual Embeddings (Transformer-based)

Utilizing models like BERT to extract embeddings where the vector for a word changes based on its surrounding context (e.g., "bank" of a river vs. financial "bank").
