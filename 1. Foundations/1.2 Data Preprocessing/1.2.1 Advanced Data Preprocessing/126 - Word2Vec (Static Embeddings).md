# Word2Vec (Static Embeddings)

A neural network-based technique that maps words to dense vectors in a continuous multi-dimensional space, capturing semantic relationships (e.g., King - Man + Woman = Queen).
