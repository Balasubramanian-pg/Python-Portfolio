# Kernel PCA

An extension of PCA using kernel methods (e.g., RBF, Polynomial) to perform non-linear dimensionality reduction by mapping data into higher-dimensional spaces before projecting it down.
