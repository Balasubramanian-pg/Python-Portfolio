# scikit-learn ColumnTransformer

A meta-estimator that allows different preprocessing pipelines to be applied concurrently to different subsets of columns (e.g., One-Hot Encoding for categorical, Scaling for continuous).
