# Autoencoders for Dimensionality Reduction

Training neural networks with a narrow "bottleneck" layer; the output of this bottleneck layer serves as a non-linear, compressed feature representation.
