# Iterative Imputer (scikit-learn)

The Python implementation inspired by MICE, which fits a sequence of regression models (like Bayesian Ridge or Extra Trees) to iteratively estimate missing values.
