# UMAP (Uniform Manifold Approximation and Projection)

A modern, fast non-linear dimensionality reduction technique that preserves both local and global data structures better than t-SNE, increasingly used in ML pipelines.
