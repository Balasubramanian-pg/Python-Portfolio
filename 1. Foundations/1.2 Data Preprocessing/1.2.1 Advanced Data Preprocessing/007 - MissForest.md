# MissForest

A non-parametric, Random Forest-based implementation of iterative imputation that naturally handles non-linear relationships, interactions, and mixed data types (categorical and continuous).
