# Non-Negative Matrix Factorization (NMF)

A dimensionality reduction technique used when data is strictly non-negative (e.g., pixel intensities, text counts), resulting in easily interpretable additive parts.
