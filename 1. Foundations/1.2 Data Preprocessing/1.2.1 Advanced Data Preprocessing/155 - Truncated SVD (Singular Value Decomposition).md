# Truncated SVD (Singular Value Decomposition)

A variant of PCA that works directly on sparse matrices (like TF-IDF matrices) without needing to dense-ify them, commonly used in NLP (Latent Semantic Analysis).
