# Binary Encoding

A hybrid approach where categories are converted to ordinal integers, which are then converted to binary code, and the digits are split into separate columns (logarithmic dimensionality).
