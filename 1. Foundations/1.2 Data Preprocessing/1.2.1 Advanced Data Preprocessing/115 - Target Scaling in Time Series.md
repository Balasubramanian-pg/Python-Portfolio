# Target Scaling in Time Series

Scaling time-series targets requires careful inverse-transformation post-prediction; parameters must be learned only on the rolling training window to prevent leakage.
