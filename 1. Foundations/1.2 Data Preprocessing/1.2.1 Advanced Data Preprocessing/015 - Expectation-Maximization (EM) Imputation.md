# Expectation-Maximization (EM) Imputation

A statistical algorithm that alternates between estimating missing values (Expectation) and optimizing model parameters (Maximization) until convergence.
