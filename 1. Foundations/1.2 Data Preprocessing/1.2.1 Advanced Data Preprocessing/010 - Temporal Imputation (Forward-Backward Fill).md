# Temporal Imputation (Forward/Backward Fill)

Advanced when combined with grouping; propagating the last known observation forward (LOCF) or next known backward (NOCB) within specific time-series entities (e.g., per user).
