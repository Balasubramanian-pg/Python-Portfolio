# Feature Hashing (Hashing Trick)

Applying a hash function to categorical strings to map them into a fixed number of integer buckets (columns), eliminating the need to maintain a vocabulary dictionary.
