# Box-Cox Transformation

A parametric power transform that automatically searches for the optimal lambda parameter to normalize data, but strictly requires all input data to be strictly positive (>0).
