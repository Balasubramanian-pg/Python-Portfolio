# Cost-Sensitive Learning (Class Weights)

Modifying the algorithm's loss function during preprocessing (via class_weight='balanced') to penalize mistakes on the minority class more severely, avoiding sampling altogether.
