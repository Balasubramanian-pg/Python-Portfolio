# Exponentially Weighted Moving Averages (EWMA)

A rolling average where more recent observations are given exponentially higher weight, responding faster to recent changes than a simple moving average.
