# Handling Unknown Categories in Production

Configuring encoders (like OneHotEncoder(handle_unknown='ignore')) to gracefully drop or map to zero any novel categorical values observed during inference.
