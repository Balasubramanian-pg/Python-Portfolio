# Smoothing in Target Encoding

Adding a regularization parameter to Target Encoding that blends the category's specific target mean with the overall global target mean based on the category's sample size.
