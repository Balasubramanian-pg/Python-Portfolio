# Reproducibility in Pipelines

Using explicit random seeds (random_state) in preprocessing steps that utilize randomness (like SMOTE, Isolation Forests, randomized imputation) to ensure deterministic pipeline execution.
